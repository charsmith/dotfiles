# dotfiles

## Install

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/charsmith/.dotfiles/master/bootstrap.sh)
```

This will install Homebrew (if needed), install required packages, clone the repo to `~/.dotfiles`, and symlink configs. Safe to re-run — already installed steps are skipped.
